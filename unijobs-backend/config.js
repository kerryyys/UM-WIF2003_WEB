// JS file for configs, I use port 5050, because youtube said so
export const PORT = 5050;

//Note that this connection is based on my own machine, your port number might be different - zhengyu
export const mongoDBConnection = "mongodb://localhost:27017/unijobs";
